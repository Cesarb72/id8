# ID.8 — CURRENT-STATE BRIEF
**Companion to: PBD v1.1 (product truth) + PDD v100 (decision truth) · Refreshed by C-suite · August 9, 2026**
**Purpose: the readable "where the build actually stands right now" snapshot. The PBD says what ID.8 IS; the PDD says what we DECIDED; this Brief says where the LIVING BUILD is today. When this Brief and the repository disagree, the repository wins on code truth and the gap is flagged.**

---

## AT A GLANCE

- **Phase:** MVP closeout — Move 3 of the authoritative 5-move sequence.
- **Governing code baseline:** branch `recovery/stage-2f-integrated-identity-lineage`, HEAD **`8db9ad1`** (advanced by exact fast-forward from `f4a23fb`; Build provenance Stage 1 integrated), clean worktree.
- **Governing record:** PDD v100 (v99 preserved as last pre-integration record). **Product truth:** PBD v1.1.
- **Move 3 = two bounded threads** (A: SOFT_FEASIBLE/Place-Right recovery; B: contextual role casting). Closes when both close.
- **MVP-green:** NOT claimed.
- **Provider valve:** closed. **Hosted/Vercel:** not entered.

## THE 5-MOVE MVP CLOSEOUT SEQUENCE — WHERE WE ARE

| Move | What it proves | Status |
|---|---|---|
| **1 — Great Stop aggregation purity** | Great Stop consumes owner-authored verdicts; re-authors nothing | CLOSED (`9029758`) |
| **2 — Experience Composition** | ID.8 authors/preserves ONE persona-specific Start->Highlight->Wind-down promise through every mutation + save/return | CLOSED (`377e051`) |
| **3 — SOFT_FEASIBLE recovery + Contextual role-casting** | (A) soft-feasible Build routes surfaced/steerable, anchor stays hard; (B) a required anchor takes its correct role by intent+location, not a forced Highlight | OPEN — two threads (see below) |
| **4 — User Transaction Lifecycle (Workstream F)** | Durable Save -> Return -> Delete; immutable share snapshot | UNSTARTED (R-04/R-05 pre-ruled) |
| **5 — Cleanup + engine-doc formalization** | Remove proven-orphan compat paths; formalize engine docs = extraction-readiness proof | UNSTARTED |

**MVP-green = all 5 closed. We are mid-Move-3.**

## MOVE 3 — TWO BOUNDED THREADS (bundled; closes when both close)

### Thread A — SOFT_FEASIBLE / Place-Right recovery
- **Scope:** authorized (C1–C4, eight-file boundary). Build-only two-action recovery ("Take the bigger night" / "Try a tighter route"); surfaces the soft/hard distinction Bearings already computes; protected stack untouched.
- **Founder tuning:** 1 of 4 locked — **F-BIG-DIMENSION** (broaden radius + transition; neighborhood continuity as preference). **F-BIG-DEGREE** = Option B provisional (pending; informed by Thread B's role work). **F-TIGHT-DIMENSION / F-TIGHT-DEGREE** not started. Final two-action UX copy returns to founder.
- **Place-Right failure OPEN:** the anchor-route Place-Right condition (`cluster_escape_structure` / `support_supply_not_buildable` / `required_stop_survival_failed`) is **this thread's condition** — not resolved.
- **Preserved diagnostic evidence:** uncommitted Thread A material in the protected original checkout (`recovery/pre-demo-mode` @ `1c36212`) — hash-preserved, byte-identical, untouched by integration (`buildCandidateAdmissibilityDiagnostics.ts`, `test-build-matrix-place-right-sidecar.ts`).
- **Implementation:** held (behind tuning).

### Thread B — Contextual role casting / provenance
- **Stage 1 (provenance):** COMPLETE + integrated at **`8db9ad1`**. Interpretation authors truthful anchor-role provenance (explicit / inferred / candidate-defaulted / missing); a missing role no longer silently defaults to `Highlight`; candidate/default evidence cannot masquerade as authoritative selected role. Post-integration proof green; Surprise/Curate byte-identical across 500 paths; provider 0.
- **Stage 2 (Waypoint role casting/selection):** HELD — requires a separately authorized bounded packet, carrying the **route-quality proof obligation** (carrier-fit != route viability; Adega-as-Start/Wind-down must actually generate a Great-Stop-passing, well-composed night) and the **cumulative Thread-B footprint review** (Stage 1's six files + Stage 2's set = still the narrow slice the goalpost was fixed at).
- **Goalpost (re-fixed):** MVP includes contextual role resolution for a required Build anchor when its role was not explicitly authored, via existing Interpretation/Taste evidence + normal Waypoint/Bearings coordination. **Full Experience Composition remains parked (post-MVP).** User-authored role stays hard and caps engine casting.

## WHAT IS CLOSED (durable, committed)

Move 1 (`9029758`) · Move 2 (`377e051`) · Product Flow Audit R-01…R-05 (#155) · R-01 (`f4a23fb`) · R-03 (`35388abf`) · Waypoint C1 correction (`6bda8eed`) · **Build provenance Stage 1 (`8db9ad1`, integrated by fast-forward)** · mode contracts (Surprise/Curate/Build v1) governing.

## RULED BUT UNIMPLEMENTED (later Moves)

- **R-02** — governed post-Lock Live adaptation (owner-engine re-authorization; LCE coordinates, never authors). Governing; unimplemented.
- **R-04 / R-05** — durable Save distinct from browser-local continuity; immutable versioned share snapshot. Move 4.

## OPEN FAILURES (neither resolved; neither a Stage 1 regression)

- **Generic-anchor runtime preservation** (`transitions` undefined) — KNOWN BASELINE FAILURE, reproduces identically on control; quarantined, separate.
- **Anchor-route Place-Right** — Thread A's condition (above); reproduces identically on control; open.

## OUTSTANDING FOUNDER DECISIONS

- Thread A tuning: **F-BIG-DEGREE** (provisional B), **F-TIGHT-DIMENSION**, **F-TIGHT-DEGREE**, + final two-action UX copy. (`cluster_escape_tolerance` excluded; dials framed in user-experience terms.)

## THE RECORD-TRUTH vs CODE-TRUTH BOUNDARY

- **PDD v100 + C-suite rulings** = decision truth. **Repository @ `8db9ad1`** = code truth. **PBD v1.1** = product truth.
- Differ on *how it works* -> code wins, gap flagged. Differ on *what we decided* -> PDD wins. PBD never becomes implementation authority.

## STANDING DISCIPLINES IN FORCE

Audit-Authorship Boundary (characterize, never author without green light — incl. record-authoring) · upgraded decision framework (1a/1b product integrity, 2a/2b can-this-human, 3 pacing; surgical-flag) · check-the-record-before-building · milestone PDD sync (also when held rulings gate real code OR the baseline advances) · trust-but-verify on protected operations.

## IMMEDIATE NEXT ACTIONS

1. **Complete the authoritative two-file record sync** — supply `ID8_PDD_v100.md` + this Brief to the governing checkout; build team runs one bounded docs-only commit; return the new docs SHA.
2. **Then** authorize Thread B Stage 2 (separate bounded packet) and continue Thread A tuning/implementation from the shared baseline `8db9ad1`.
3. Do NOT describe either open failure as resolved; do NOT close Move 3; do NOT claim MVP-green.

---

*Point-in-time snapshot; refreshed at each Move close, major ruling, or baseline advance. PDD v100 carries authoritative decision detail; PBD v1.1 carries durable product truth; this Brief carries current build position.*
